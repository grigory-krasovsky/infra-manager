-- Откат V5: число незакрытых задач больше не наблюдается.
--
-- Колонку карточки двигает только статус ревьюера: NEEDS_WORK, апрув или ничего.
-- Задачи и комментарии статус в Bitbucket не меняют, и мы решили их не читать —
-- колонка отвечает на вопрос "чей ход", а что происходит внутри ревью, остаётся
-- ответственностью ревьюера.
--
-- Отдельной миграцией, а не удалением V5: та уже применена на работающей базе, и
-- исчезнувший файл Flyway считает расхождением и отказывается стартовать.
ALTER TABLE pr_poll_state
    DROP COLUMN IF EXISTS open_tasks;
